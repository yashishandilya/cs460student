# cs460student

Prompt for index.html:
"In a file index.html, create an interactive white cube, using XTK framework and pure HTML, want minimum functionality, no exra features on top of what is asked, keep UI to the minimum as well"

Prompt for bonus.html:
"now create a file bonus.html and utilise three.js in place of xtk framework, and pure HTML, want minimum functionality, no extra features on top of what is asked, keep UI to the minimum as well, add interactivity so that I am able to press and drag on cube and it rotates or spins?"

Between bonus.html (using THREE.js) and index.html (using HTK), had to explicitly mention 'adding interactivity' for bonus.html using Object Controls. This is generally visible as well where Three.js offers more low-level control with geomery and shading, requiring more setup and code; XTK provides the 3D behavior out of the box.